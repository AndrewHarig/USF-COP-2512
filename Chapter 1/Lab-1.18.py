#takes the first line as an input and sets it as a string named first_name
first_name = input()

#takes the second line as an input and sets it as a string named generic_location
generic_location = input()

#takes the third line as an input and sets it as and integer named whole_number
whole_number = int(input())

#takes the fourth line as an input and sets it as and string named plural_noun
plural_noun = input()

#print statement using the variablesto create a sentence
print(first_name, 'went to', generic_location, 'to buy', whole_number, 'different types of', plural_noun)
